# Ava — Albion Mutual · Final Test Report

**Date:** 31 May 2026
**App:** https://ava-albion-mutual.pages.dev
**Commit:** c0a9f3e
**Run:** local production build (`vite preview`), all n8n calls mocked

## ✅ Result: 34 / 34 tests passing (0 failures)

| Suite | Tests | Passed | Failed | Time |
|---|---|---|---|---|
| Unit (Vitest, jsdom) | 22 | 22 | 0 | ~1.1s |
| E2E (Playwright, Chromium) | 12 | 12 | 0 | 15.5s |
| **Total** | **34** | **34** | **0** | |

## Unit tests — business logic (`src/logic.js`)
- **routeClass** — auto / fraud / adjuster badge mapping (3 cases)
- **meterColor** — fraud-risk colour bands: red ≥60, amber ≥30, green (3 cases)
- **slaInfo** — 24h SLA countdown: N/A, invalid date, overdue, warn, ok (6 cases)
- **stageIndex** — claim lifecycle stage mapping (2 cases)
- **adminMetrics** — Admin-Ava analytics: totals, 7/30-day windows, decisions,
  in-review/paid/vulnerable, fraud bands, by-type buckets, value sums, empty-list
  safety (7 cases)
- **localAnswer** — Admin-Ava plain-text fallback (1 case)

## E2E tests — frontend flows (backend mocked; never touches live n8n / DB / email)
1. Overview loads with product branding
2. Ava demo page is gated by email sign-in → chat reveals after sign-in
3. How It Works routes to the gated demo (no ungated Ava)
4. Data & Security page renders governance content
5. Assignment page embeds the official Jeen brief PDF
6. Presentation renders the official deck PDF (not inline slides)
7. Admin console login → MI dashboard + "Ask Ava" analytics tab
8. Team console renders its login
9. Database page embeds the live CloudBeaver panel
10. Observability page shows the Grafana panel + live link
11. My Claims portal loads claims for an email (fallback auth)
12. Footer no longer exposes navada-edge-server.uk / github links

## Defects found
None in the application. Two issues surfaced during authoring, both **test-side**:
- Initial Playwright selectors were ambiguous under strict mode → tightened.
- The "Talk to Ava" button selector matched two elements → made exact.

Both fixed and re-verified. The team-login failure reported earlier was traced
to the window when Postgres was disconnected; with the DB now connected,
adjuster / legal / finance all authenticate (verified against the live backend).

## How to run
```bash
cd react-app
npm run test:unit   # 22 unit tests (Vitest)
npm run test:e2e    # 12 e2e tests (Playwright)
```

## Outstanding before the demo
- **Grafana image:** the Observability page references `/shots/grafana-hero.png`
  — add a real Grafana screenshot there before the final deploy (otherwise that
  one image is broken).
- **Deploy:** latest commit is on GitHub but the live site was last deployed at
  `index-BbA116Uu.js`. Run `cd react-app && npx wrangler pages deploy dist
  --project-name=ava-albion-mutual --branch=main` once the Grafana image is in.
- **Manual click-through:** recommend a final human pass of the live site
  (email gate → returning-user memory → Admin "Ask Ava" with real numbers),
  since the e2e suite mocks the backend by design.

— Automated report generated by Claude Code
